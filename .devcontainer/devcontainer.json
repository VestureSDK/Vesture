{
	"name": "Ingot",
	"build": { "dockerfile": "../build/docker/Dockerfile" },
	"postStartCommand":  "dotnet tool restore && dotnet ib setup",
	"remoteUser": "root",
	"containerEnv": {
		"IS_DEVCONTAINER": "true"
	}
}
