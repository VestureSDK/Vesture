name: 'Ingot Setup'
description: 'Performs Git checkout and sets up the environment for the Ingot project'
author: 'Ingot'
runs:
  using: 'composite'
  steps:
    - shell: pwsh
      name: Environment Setup
      run: |
        dotnet tool restore
        dotnet ib Setup