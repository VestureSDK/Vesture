name: Ingot
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
jobs:
  libs:
    name: Libraries
    runs-on: ubuntu-latest
    container: mcr.microsoft.com/dotnet/sdk:9.0
    steps:
      - uses: actions/checkout@v4
      - name: Setup
        run: |
          dotnet tool restore
          dotnet ib Setup
      - name: Build
        run: dotnet ib Build
      - name: Test
        run: dotnet ib Test
      - name: Validate
        run: dotnet ib Validate
      - name: Release
        run: dotnet ib Release
